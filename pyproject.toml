[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "meridian-vpn"
dynamic = ["version"]
description = "Deploy a censorship-resistant proxy server — hardened by default, shared via QR code"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [{ name = "uburuntu" }]
keywords = ["vpn", "proxy", "vless", "reality", "xray"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Topic :: Internet :: Proxy Servers",
    "Topic :: System :: Networking",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "typer>=0.12",
    "rich>=13.0",
    "pyyaml>=6.0",
    "packaging>=23.0",
    "jinja2>=3.0",
    "segno>=1.5",
    "httpx>=0.27",
    "pydantic>=2.11",
    "fastapi>=0.128",
    "uvicorn>=0.30",
    "remnawave==2.7.1",
]

[project.scripts]
meridian = "meridian.cli:app"

[project.urls]
Homepage = "https://getmeridian.org"
Repository = "https://github.com/uburuntu/meridian"
Issues = "https://github.com/uburuntu/meridian/issues"

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.4", "mypy>=1.0", "types-PyYAML>=6.0"]
# Cloud providers for `meridian.infra` — used by tests/realvm/ harness and
# (eventually) by `meridian deploy --create-vm <provider>`. Each provider is
# optional: `pip install meridian-vpn[hetzner]` gets only Hetzner support.
hetzner = ["hcloud>=2.0"]

[tool.hatch.version]
path = "VERSION"
pattern = "(?P<version>.+)"

[tool.hatch.build.targets.wheel]
packages = ["src/meridian"]

[tool.hatch.build.targets.wheel.force-include]
"website/dist" = "meridian/studio_assets"

[tool.hatch.build.targets.sdist]
exclude = [
    "/.github",
    "/docs",
    "/website",
    "/tests",
    "/dist",
    "*.tar",
    "*.tar.gz",
    "/docker-compose.test.yml",
    "/context7.json",
    "/Makefile",
    "/BACKLOG.md",
    "/CODE_OF_CONDUCT.md",
    "/CONTRIBUTING.md",
    "/SECURITY.md",
    "/uv.lock",
    "CLAUDE.md",
]

[tool.hatch.build.targets.sdist.force-include]
"website/dist" = "src/meridian/studio_assets"

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "W", "TID251"]

[tool.ruff.lint.flake8-tidy-imports.banned-api]
# Layer boundaries enforced at lint time — complement tests/test_architecture.py.
# core/ must never depend on CLI, console, or Rich.
"meridian.console".msg = "core/ and engine/ must not import meridian.console — keep UI in adapters"
"meridian.commands".msg = "Do not import from commands/ in library modules — extract shared logic instead"
"rich".msg = "Do not import Rich in core/ or engine/ — keep rendering in CLI adapters"

[tool.ruff.lint.per-file-ignores]
# Only core/ and engine/ are banned from console/Rich — command modules need them.
"src/meridian/commands/*" = ["TID251"]
"src/meridian/panel_bootstrap.py" = ["TID251"]
"src/meridian/relay_ops.py" = ["TID251"]
"src/meridian/ssh.py" = ["TID251"]
"src/meridian/provision/*" = ["TID251"]
"src/meridian/operations.py" = ["TID251"]
"src/meridian/console.py" = ["TID251"]
"src/meridian/cli.py" = ["TID251"]
"src/meridian/render.py" = ["TID251"]
"src/meridian/display.py" = ["TID251"]
"src/meridian/pwa.py" = ["TID251"]
"src/meridian/branding.py" = ["TID251"]
"src/meridian/update.py" = ["TID251"]
"src/meridian/ai.py" = ["TID251"]
"src/meridian/reconciler/display.py" = ["TID251"]
"src/meridian/reconciler/prepare.py" = ["TID251"]
"tests/*" = ["TID251"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# tests/realvm/ provisions real cloud VMs — opt-in, never auto-collected.
# Invoke manually via `make real-lab` (see tests/realvm/README.md).
# tests/systemlab/ uses Docker, orchestrated via `make system-lab`.
norecursedirs = ["realvm", "systemlab"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
