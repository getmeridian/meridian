{
  "$defs": {
    "VerificationCounts": {
      "additionalProperties": false,
      "description": "Aggregate check counts by terminal status.",
      "properties": {
        "checks": {
          "minimum": 0,
          "title": "Checks",
          "type": "integer"
        },
        "failed": {
          "minimum": 0,
          "title": "Failed",
          "type": "integer"
        },
        "passed": {
          "minimum": 0,
          "title": "Passed",
          "type": "integer"
        },
        "skipped": {
          "minimum": 0,
          "title": "Skipped",
          "type": "integer"
        },
        "warnings": {
          "minimum": 0,
          "title": "Warnings",
          "type": "integer"
        }
      },
      "required": [
        "checks",
        "passed",
        "failed",
        "warnings",
        "skipped"
      ],
      "title": "VerificationCounts",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "Purely derived verification verdict, counts, and completed exit code.",
  "properties": {
    "counts": {
      "$ref": "#/$defs/VerificationCounts"
    },
    "exit_code": {
      "enum": [
        0,
        3,
        4
      ],
      "title": "Exit Code",
      "type": "integer"
    },
    "verdict": {
      "enum": [
        "passed",
        "findings",
        "inconclusive"
      ],
      "title": "Verdict",
      "type": "string"
    }
  },
  "required": [
    "verdict",
    "exit_code",
    "counts"
  ],
  "title": "VerificationAggregate",
  "type": "object"
}
